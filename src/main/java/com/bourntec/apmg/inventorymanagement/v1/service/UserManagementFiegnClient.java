package com.bourntec.apmg.inventorymanagement.v1.service;

//import org.springframework.cloud.openfeign.FeignClient;
//import org.springframework.web.bind.annotation.GetMapping;
//import org.springframework.web.bind.annotation.PathVariable;

//@FeignClient(name = "inventorymanagement")
public interface UserManagementFiegnClient {
	/**
	 * Interface method to get the greetings information from a different
	 * microservice.
	 * 
	 * @param langCode
	 * @return
	 */
	//@GetMapping(value = "/inventorymanagement/v1/bankaccount/{bankNo}")
	//public void getGreetings(@PathVariable(name = "bankNo") String bankNo);
}
